/target
**/*.rs.bk
.DS_Store

# ripple indexes itself now (see .claude/skills/use-ripple). `**` so a stray index
# under a crate subdir (e.g. a daemon demand-load) is ignored too.
/.ripple/
**/.ripple/

# docs site build artifacts
/docs/node_modules/
/docs/dist/
/docs/.astro/

/.hallmark/
