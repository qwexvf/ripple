; HTML is a host format: it defines no code symbols of its own.
; Its value to ripple is the embedded <script> block, which
; `embedded_regions` hands to the TypeScript adapter. This query
; intentionally captures nothing.
