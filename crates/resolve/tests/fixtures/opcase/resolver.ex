defmodule App.Resolvers.PlayerResolver do
  def me(_, _, _) do
    :ok
  end

  def follow(_, _, _) do
    :ok
  end

  def legacy(_, _, _) do
    :ok
  end

  def decoy(_, _, _) do
    :ok
  end
end
